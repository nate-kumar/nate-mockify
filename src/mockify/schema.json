{
  "$schema": "http://json-schema.org/schema",
  "id": "Mockify",
  "title": "Mockify Options Schema",
  "description": "Generates mock file for a given TypeScript model",
  "properties": {
    "modelsFolderUrl": {
      "type": "string",
      "description": "The url of the .model.ts files",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Please specify the relative path where .model.ts files are located\n  > Default path: ./models\n"
    },
    "mocksFolderUrl": {
      "type": "string",
      "description": "The url of the .model.ts files",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-prompt": "Please specify the relative path where .model.mock.ts files are to be generated\n  > Default path: ./models/mocks\n"
    },
    "overwriteExisting": {
      "type": "string",
      "description": "The url of the .model.ts files",
      "$default": {
        "$source": "argv",
        "index": 2
      }
    }
  },
  "required": [
    "modelsFolderUrl",
    "mocksFolderUrl"
  ]
}